(ocamllex (modules rx_lexer))
(menhir (modules rx_parser))

(env
  (dev
    (flags (:standard -warn-error -A))))

(library
  (name nerode_regular)
  (public_name nerode.regular)
  (libraries yojson nice_parser core))

(documentation
  (package nerode)
  (mld_files :standard))
